<tr>
<td><%= blog.title.upcase %></td>
<td><%= blog.body %></td>
<td><%= link_to blog.status, portfolio_status_blog_path(blog) %></td>
<td><%= link_to 'Show', blog %></td>
<td><%= link_to 'Edit', edit_blog_path(blog)  if logged_in?(:site_admin) %></td>
<td><%= link_to 'Delete Blog', blog, method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin)  %></td>
</tr>