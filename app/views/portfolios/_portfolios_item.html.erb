<p><%= link_to portfolios_item.title, portfolio_show_path(portfolios_item) %></p>
<p><%= portfolios_item.subtitle %></p>
<p><%= portfolios_item.body %></p>
<p><%= image_tag portfolios_item.thumb_image unless portfolios_item.thumb_image.nil? %></p>
<p><%#= link_to "Edit", edit_portfolio_path(portfolios_item.id) %></p>
<p><%= link_to "Edit", edit_portfolio_path(portfolios_item) if logged_in?(:site_admin) %></p>
<p><%= link_to 'Delete Portfolio', portfolio_path(portfolios_item.id), method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin) %></p>